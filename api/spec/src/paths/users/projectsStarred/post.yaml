summary: "Add project as starred by users"
operationId: "addStarredProject"
consumes:
- "application/json"
produces:
- "application/json"
parameters:
- name: "userId"
  in: "path"
  description: "ID of users to get projects from"
  required: true
  type: string
responses:
  409:
    description: "Project already starred"
  200:
    description: "OK"
    schema:
      type: array
      items:
        $ref: "file://src/open_api.yaml#/definitions/ProjectDefault"